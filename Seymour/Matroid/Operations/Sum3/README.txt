This folder is temporary.

After finishing all proofs about 3-sum, concatenate files from this folder into the file Sum3.lean one folder higher.

After concatenating the files, replace all `/-private-/` by `private` for the desired result.
